# Archived / not-in-use material

Items here are **not part of the active** `backend/` + `frontend/` product path. Keep for reference or occasional legacy video/ML work.

| Path | What it was |
|------|-------------|
| `not-in-use/` | See below |

## `not-in-use/`

| Item | Note |
|------|------|
| `dlib-…-win_amd64.whl` | Old Windows wheel for dlib (CP39). Current app does not require it in the slim API path; pin dlib in env if you enable video features. |
| `requirements copy.txt` | Duplicate snapshot of an old `requirements` file. |
| `backend_test_legacy/` | Standalone driver script for body-language experiments (not imported by FastAPI). |

If you still see a stray `src/` folder at the **repository root** (leftover layout), close IDEs using it and delete or move that folder here manually.
