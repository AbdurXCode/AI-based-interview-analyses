*.egg-info/
__pycache__/
*.py[cod]
*.pyo

.venv/
venv/
Gaze_Environment/

.env
.env.*
!.env.example

# SSH keys / certs (never commit EC2 `.pem`, etc.)
*.pem

assets/*.mp4
assets/*.wav
assets/coords_pred.csv
chunks_test/
tmp/

*.whl
*.zip
temp_video.mp4

frontend/.next/
frontend/node_modules/
frontend/tsconfig.tsbuildinfo

backend/.venv/
backend/uploads/
backend/**/*.log

# Local / generated uploads (relative paths vary by cwd)
uploads/

# OS noise
.DS_Store
Thumbs.db

# Tests / coverage
.pytest_cache/
.coverage
htmlcov/

# ── Heavy ML binaries (prefer Git LFS or a documented download script) ─────────
backend/models/pretrained_models/*.h5
backend/models/pretrained_models/*.dat
backend/models/pretrained_models/*.pkl

# Personal scratch (don’t ship in the repo); remove the line below if you add real sample assets
/*.pdf
